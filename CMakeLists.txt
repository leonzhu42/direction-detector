cmake_minimum_required(VERSION 2.8)
project( direction_detector )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( direction_detector direction_detector.cpp helper.cpp helper.h )
target_link_libraries( direction_detector ${OpenCV_LIBS} )